# =======================================================
# = RABBITMQ
# =======================================================
library.notification.exchange=library.notification.exchange
library.notification.queue=library.notification.queue
notification.routing.key=notification.routing.key

# =======================================================
# = DATASOURCE
# =======================================================

spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:db;TRACE_LEVEL_FILE=4;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=sa


# =======================================================
# = JPA / HIBERNATE
# =======================================================

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

logging.level.org.hibernate.SQL=INFO
logging.level.org.springframework.transaction=INFO
logging.level.org.hibernate.type.descriptor.sql=INFO

# ==========================================================
# - APPLICATION PROPERTIES
# ==========================================================

books.reservation.users.max=5
books.reservation.days.max=10
books.reservation.fine.price=8.00

jwt.secret=tav0001++@
jwt.expiration.seconds=3

library.notification.server.port=3002